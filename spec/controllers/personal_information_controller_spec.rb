require "rails_helper"

describe PersonalInformationController do
  describe "#update" do
    context "valid params" do
      it "persists the params to the model" do

      end
    end

    context "invalid params" do
      it "displays a validation error" do

      end
    end
  end
end